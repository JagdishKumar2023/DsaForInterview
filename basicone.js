let str = "Hello, how are you"; // String is connvernt in array
console.warn(str.split());

let String = "Hello from hello"; // To add the value in the h for _
console.log(str.replace("h", "_"));

let stringSub = "Let go to new world"; // To find out the value of to between 6 to 10
console.log(stringSub.substring(6, 10));

let stringDestruture = "Destureture the string"; // To destructure the string in to array
console.log([...stringDestruture]);

let strAdd = "Adding a string in more line"; // To add the line in and reverse the value and  join the string
let temp = strAdd.split("").reverse("").join("");
console.log(temp);

let strBlankSpace = "  Hello  "; // Trim remove the extra space in sting

console.log(strBlankSpace.trim());
